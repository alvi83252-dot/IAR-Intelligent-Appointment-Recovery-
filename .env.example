# =============================================================================
# IAR — Environment template (safe to commit). Copy to .env and fill in values:
#     cp .env.example .env        # PowerShell: Copy-Item .env.example .env
# .env is gitignored; never commit a real key.
# =============================================================================

# --- LLM provider ------------------------------------------------------------
# Google Gemini API key. Get one at https://aistudio.google.com/apikey
GEMINI_API_KEY=your-gemini-api-key-here

# Provider + default model (used when an agent has no specific override).
LLM_PROVIDER=gemini
LLM_MODEL=gemini-3.5-flash

# --- Per-agent model selection ----------------------------------------------
# Personal agent and Front Desk (customer service) agent run on Gemini Flash.
PERSONAL_AGENT_MODEL=gemini-3.5-flash
FRONTDESK_AGENT_MODEL=gemini-3.5-flash
# Research agent runs the stronger reasoning model for safety-critical triage.
RESEARCH_AGENT_MODEL=gemini-3.1-pro
# Optional cheaper/faster model for the disruption batch re-score.
RESEARCH_BATCH_MODEL=gemini-3.5-flash

# --- A2A service URLs (used once the Python agent servers exist) -------------
PERSONAL_AGENT_URL=http://localhost:9001/
FRONTDESK_AGENT_URL=http://localhost:9002/
RESEARCH_AGENT_URL=http://localhost:9003/

# --- Research grounding (Linkup web search for find_alternatives) -------------
# Linkup API key — https://app.linkup.so. Used ONLY for find_alternatives
# signposting, restricted to official NHS website sources. Blank => mock directory.
LINKUP_API_KEY=your-linkup-api-key-here
